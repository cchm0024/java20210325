package P15.lecture;

public class A01CollectionFramework {
	// collection: 인스턴스를 저장한 인스턴스(클래스)
	
	// data를 저장하는 방법 : 자료구조(data structure)
	
	// collection framework
	// 자주 사용하는 자료구조에  관련된 
	// 클래스(인스턴스 설계도)와 인터페이스, 메소드들 묶음
	
	// 대표인터페이스
	// List (순서 있고, 중복 저장 가능)
	// Set  (순서없고, 중복 불가)
	// Map  (키, 값)쌍으로 저장
}
